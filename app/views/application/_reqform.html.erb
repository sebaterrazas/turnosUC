<%= form_with(model: request, local: true) do |form| %>
  <% if request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request.errors.count, "error") %> prohibited this request from being saved:</h2>

      <ul>
        <% request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id, value: current_user.id.to_i%>
  </div>

  <div class="field">
    <%= form.hidden_field :turno_id, value: @turno.id.to_i%>
  </div>

  <div class="field">
    <%= form.hidden_field :state, value: 'Pending'%>
  </div>


  <div class="field is-rounded" align="center">
    <div>
        <b>Descripción</b> <i>(Opcional)</i><br>
    </div>
    <%= form.text_field :description, :class=>"mt-3 input is-rounded"%>
  </div>

  <div class="actions" align="center">
    <%= form.submit "Enviar solicitud", :class=>"button is-warning has-text-info mt-3"%>
  
  </div>
<% end %>
